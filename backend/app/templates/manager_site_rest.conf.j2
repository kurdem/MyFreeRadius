[# sites-enabled/manager - generated by FreeRADIUS Manager. DO NOT EDIT BY HAND. #]
# =============================================================================
# Virtual server "manager" (MFA mode) - generated [[ generated_at ]]
# Delegates authentication to the FreeRADIUS Manager backend via rlm_rest, which
# verifies the TOTP second factor (and, in append mode, the AD password and
# group membership). Clients are routed here via `virtual_server = manager`.
# Managed by FreeRADIUS Manager.
# =============================================================================
server manager {
    authorize {
        preprocess

        # Delegate the decision to the backend.
        update control {
            Auth-Type := rest
        }
    }

    authenticate {
        Auth-Type rest {
            rest
        }
    }

    preacct {
        preprocess
    }

    accounting {
    }

    post-auth {
        Post-Auth-Type REJECT {
        }
    }
}
